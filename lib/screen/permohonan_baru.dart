import 'package:flutter/material.dart';
import 'dart:ui';

class PermohonanBaru extends StatelessWidget {
  const PermohonanBaru({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: Center(
          child: Text("Permohonan Baru Page"),
        ));
  }
}
